# Copy to `.env` and fill in the keys for whichever providers you use.
# `circuit-extract` loads this automatically via python-dotenv.

GEMINI_API_KEY=
# GOOGLE_API_KEY=  # alternate name accepted by google-genai
